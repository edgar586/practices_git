
# feature/dev6 — ejemplo de cambio conflictivo

Sugerencia (hacer en `feature/dev6`):

1. Edita `ConflictProneService.buildMessage()`.
2. Modifica la línea de retorno por ejemplo:
   `return "Feature DEV6: mejora UI para formulario de login";`

Resultado esperado: cuando varias ramas modifiquen la misma línea, verás conflictos al hacer merge.
