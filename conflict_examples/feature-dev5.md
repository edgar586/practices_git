
# feature/dev5 — ejemplo de cambio conflictivo

Paso a paso (hacer en `feature/dev5`):

1. Abre `src/main/java/com/example/demo/conflict/ConflictProneService.java`.
2. Cambia la línea `return ...` por ejemplo:
   `return "Feature DEV5: soporte para bloqueo de cuenta";`

Objetivo: forzar un conflicto directo con otras ramas que modifiquen la misma línea.
