
# feature/dev3 — ejemplo de cambio conflictivo

Cambios propuestos (hacer en `feature/dev3`):

1. Abre `ConflictProneService.java`.
2. Cambia la línea de retorno por ejemplo:
   `return "Feature DEV3: integra autenticación con token";`

Nota:
- El objetivo es que todas las ramas modifiquen exactamente la misma línea para forzar conflictos.
